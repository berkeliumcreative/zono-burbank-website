// @ts-nocheck
"use client";

// Placeholder page.tsx. The leadgen pipeline overwrites this on every deploy
// with a fully bespoke page (see /generate-site skill).

export default function Page() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="max-w-md text-center">
        <h1 className="text-3xl font-semibold mb-3">Scaffold ready</h1>
        <p className="text-sm text-neutral-500">
          This page is replaced on first deploy.
        </p>
      </div>
    </main>
  );
}
